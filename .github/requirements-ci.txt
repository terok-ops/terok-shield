# Pinned CI-only tool versions (managed by dependabot via pip ecosystem).
# Keep in sync with the caret constraints in pyproject.toml [tool.poetry.group.dev.dependencies].
docstr-coverage>=2.3.2,<3.0.0
vulture>=2.12,<3.0.0
bandit>=1.7,<2.0.0
